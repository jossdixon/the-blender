<div class="container">
<%= render 'search'%>

<h1><%= "#{@profile.user.first_name} #{@profile.user.last_name}" %></h1>

<%= render 'payment_popup', loanee: @profile.user.loans[-1].loanee, weekly_payment: @weekly_payment %>
</div>
